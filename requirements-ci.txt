prometheus-client==0.26.0
psycopg2-binary==2.9.12
requests==2.34.2
boto3==1.43.85
moto==5.2.3
pytest==9.1.1
pytest-cov==7.1.0
PyYAML==6.0.3
ruff==0.16.5
# Serving API. fastapi/pydantic build the app and its response models; httpx backs FastAPI's
# TestClient. The `trino` driver is deliberately NOT here -- api/src/trino_client.py imports it
# lazily inside connect_from_env, so the whole API test suite runs with no driver and no
# warehouse. Keeping it out of CI is what proves that.
fastapi==0.141.1
pydantic==2.13.5
httpx==0.28.1
